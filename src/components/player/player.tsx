import { PlayerContent } from "./playerContent";

export const Player = () => {
  return (
    <div className="relative bg-black w-full py-2 h-[80px] px-4">
      <PlayerContent />
    </div>
  );
};
